"""Zephyra Lite — ORM models.

Deliberately empty at Milestone 0. :class:`Base` is re-exported here so the
conversation, message and task tables have one obvious home in Milestone 1.
"""

from app.db import Base

__all__ = ["Base"]
